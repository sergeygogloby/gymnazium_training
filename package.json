{
  "name": "gymnazium-training",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix server & npm run dev --prefix app",
    "dev:app": "npm run dev --prefix app",
    "dev:server": "npm run dev --prefix server",
    "build": "npm run build --prefix app",
    "test": "npm run test --prefix server && npm run test --prefix app",
    "init-db": "npm install --prefix server && npm run init-db --prefix server",
    "publish-all": "npm install --prefix server && npm run publish-all --prefix server",
    "import-csv": "npm install --prefix server && npm run import-csv --prefix server --",
    "t5-critical-path": "npm install --prefix server && npm run t5-critical-path --prefix server",
    "install:all": "npm install --prefix app && npm install --prefix server"
  }
}
